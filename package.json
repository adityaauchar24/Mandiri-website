{
  "name": "mandiri-fullstack-cyclic",
  "version": "1.0.0",
  "description": "PT. International Mandiri Expo - Full Stack Deployment on Cyclic",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd mandiri-project-backend && npm run dev",
    "client:dev": "cd mandiri-project-frontend && npm start",
    "build": "node build.js",
    "install-all": "npm install && cd mandiri-project-backend && npm install && cd ../mandiri-project-frontend && npm install",
    "cyclic-build": "npm run build",
    "heroku-postbuild": "cd mandiri-project-frontend && npm install && npm run build"
  },
  "keywords": [
    "mern",
    "fullstack",
    "cyclic",
    "react",
    "nodejs",
    "mongodb"
  ],
  "author": "PT. International Mandiri Expo",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^6.8.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "fs-extra": "^11.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}